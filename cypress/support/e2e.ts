// Cypress support file
// Adds Testing Library commands for better querying
import '@testing-library/cypress/add-commands';

// Optionally, you can add custom commands here
// e.g., Cypress.Commands.add('login', () => {...});
